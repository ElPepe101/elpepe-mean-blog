/* eslint-env mocha */

/*
import 'chai/should'

import {$q, $window} from 'angular'
import {$httpBackend} from'angular-mocks'

import HomeController from 'HomeController.controller'
import PostsService from 'PostsService.service'
import AuthService from 'AuthService.service'

describe('Home controller', function () {
  var home

  beforeEach(function () {
    // Inject services
    AuthService = new AuthService($httpBackend, $q, $window)
    PostsService = new PostsService($httpBackend, AuthService)
    home = new HomeController(PostsService, AuthService)
  })
})
*/

/*
it('should change value to one in [John, Elisa, Mark, Annie] on randomName', function () {
  home.randomName()
  console.log(home.name)
  expect(home.name).to.match(/\bJohn\b|\bElisa\b|\bMark\b|\bAnnie\b/)
})
 */
